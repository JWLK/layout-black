import Image from 'next/image';
import { MQLWrap } from '@styles/mediaquery';

//CSS
import { Header, VeticalCenter } from '@styles/index';

const Main = () => {
    return (
        <>
            <Header css={VeticalCenter}>test</Header>
        </>
    );
};

export default Main;
